package test:logging@1.0.0;

interface system {
    println: func(msg: string);
}

interface logger {
    use test:types/types@1.0.0.{level};

    log: func(lvl: level, msg: string);
}

world logging {
    import system;
    export logger;
}