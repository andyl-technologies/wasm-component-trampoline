[package]
name = "runner"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
semver = "1.0"
wac-trampoline = { path = "../.." }

[target.'cfg(unix)'.dependencies]
tokio = { version = "1.0", features = ["full"] }
wasmtime = { version = "32", features = ["component-model", "async"] }
wasmtime-wasi = { version = "32" }
